{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "vnetName": {
            "type": "string",
            "defaultValue": "myVNet",
            "metadata": {
                "description": "Name of the VNet."
            }
        },
        "vnetAddressPrefix": {
            "type": "string",
            "metadata": {
                "description": "Address prefix for the VNet in CIDR format."
            }
        },
        "subnetCount": {
            "type": "int",
            "metadata": {
                "description": "Number of subnets to create."
            }
        }
    },
    "variables": {
        "subnetPrefix": "[concat(variables('vnetAddressPrefix').split('/')[0],'/',32-27)]"
    },
    "resources": [
        {
            "type": "Microsoft.Network/virtualNetworks",
            "apiVersion": "2021-06-01",
            "name": "[parameters('vnetName')]",
            "location": "[resourceGroup().location]",
            "properties": {
                "addressSpace": {
                    "addressPrefixes": [
                        "[parameters('vnetAddressPrefix')]"
                    ]
                },
                "subnets": [
                    {
                        "copy": {
                            "name": "subnetLoop",
                            "count": "[parameters('subnetCount')]",
                            "input": {
                                "name": "subnet[copyIndex()]",
                                "properties": {
                                    "addressPrefix": "[concat(variables('subnetPrefix'), '/', 27, '.', copyIndex())]"
                                }
                            }
                        }
                    }
                ]
            }
        }
    ]
}
